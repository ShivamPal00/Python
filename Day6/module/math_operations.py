# Create a Python module named math_operations that contains the following functions:
# add(a, b): Returns the sum of a and b.
# subtract(a, b): Returns the difference between a and b.
# multiply(a, b): Returns the product of a and b.
# divide(a, b): Returns the division of a by b.
# Write a separate Python program that imports the math_operations module and uses its functions to perform arithmetic operations on user-provided numbers.


import math_custom_module as m

print(m.add(2,3))
print(m.sub(3,2))
print(m.mul(2,3))
print(m.div(3,2))
